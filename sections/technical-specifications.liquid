<style>
  .technical-specs-section {
    padding: {{ section.settings.padding_top_desktop }}px 0 {{ section.settings.padding_bottom_desktop }}px;
    max-width: {{ section.settings.container_width }}px !important;
    margin: 0 auto;
  }
  .section-technical-specifications {
    background-color: {{ section.settings.background_color }};
    

  }

  .technical-specs-container {

    margin: 0 auto;
    padding: 0 20px;
  }

  .technical-specs-title {
    font-size: {{ section.settings.title_font_size_desktop }}px;
    line-height: {{ section.settings.title_line_height_desktop }};
    font-weight: 300;
    margin-bottom: 40px;
    color: #333;
  }

  .technical-specs-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    margin-bottom: 0;
  }

  .specs-left {
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  .specs-subtitle {
    font-size: {{ section.settings.subtitle_font_size_desktop }}px;
    line-height: {{ section.settings.subtitle_line_height_desktop }};
    font-weight: 300;
    margin-bottom: 20px;
    color: inherit;
  }

  .specs-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px 60px;
  }

  .spec-item {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .spec-label {
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .spec-value {
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
  }

  .specs-right {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .specs-image {
    max-width: 100%;
    height: auto;
  }

  /* Tabs Section */
  .specs-tabs-wrapper {
    background: transparent;
    margin-top: 0;
    margin-bottom: 40px;
  }

  .specs-tabs {
    display: flex;
    gap: 40px;
    border-bottom: 1px solid #e0e0e0;
    margin-bottom: 0;
    justify-content: space-between;
  }

  .specs-tab {
    padding: 15px 0;
    font-size: {{ section.settings.tab_font_size_desktop }}px;
    line-height: {{ section.settings.tab_line_height_desktop }};
    font-weight: 400;
    color: #000;
    cursor: pointer;
    border-bottom: 2px solid transparent;
    transition: all 0.3s ease;
    background: none;
    border-top: none;
    border-left: none;
    border-right: none;
  }



  .specs-tab.active {

    border-bottom-color: #333;
  }

  .specs-tab-content {
    display: none;
    padding: 40px 30px;
  }

  .specs-tab-content.active {
    display: block;
  }

  /* PDF Downloads */
  .download-list {
    display: flex;
    flex-direction: column;
  }

  .download-item {
    border-bottom: 1px solid #e0e0e0;
  }

  .download-item:last-child {
    border-bottom: none;
  }

  .download-link {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 0;
    text-decoration: none;
    color: inherit;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .download-link:hover {
    opacity: 0.8;
  }

  .download-label {
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
    color: inherit;
  }

  .download-button {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 20px;
    background: transparent;
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
    color: inherit;
    opacity: 0.7;
    transition: all 0.3s ease;
    text-decoration: none;
    pointer-events: none;
  }

  .download-link:hover .download-button {
    opacity: 1;
  }


  .download-icon {
    width: 19px;
    height: 19px;
    flex-shrink: 0;
  }

  .tab-content-title {
    font-size: {{ section.settings.subtitle_font_size_desktop }}px;
    line-height: {{ section.settings.subtitle_line_height_desktop }};
    font-weight: 300;
    margin-bottom: 15px;
    color: inherit;
  }

  /* FAQ */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .faq-item {
    border-bottom: 1px solid #e0e0e0;
  }

  .faq-question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px 0;
    background: none;
    border: none;
    text-align: left;
    cursor: pointer;
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
    font-weight: 400;
    letter-spacing: 0.5px;
    transition: color 0.3s ease;
  }


  .faq-icon {
    transition: transform 0.3s ease;
    font-size: 18px;
  }

  .faq-item.active .faq-icon {
    transform: rotate(90deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
  }

  .faq-answer-content {
    padding-bottom: 24px;
    margin-left: 2rem;
  }

  .faq-item.active .faq-answer {
    max-height: 500px;
  }

  /* Video */
  .video-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 30px;
  }

  .video-item {
    position: relative;
    border-radius: 8px;
    overflow: hidden;
  }

  .video-item .video-thumbnail-wrapper {
    cursor: pointer;
  }

  .video-thumbnail-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 16/9;
  }

  .video-thumbnail {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .video-play-button {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 70px;
    height: 70px;
    background: rgba(255, 255, 255, 0.95);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .video-play-button::after {
    content: '';
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 12px 0 12px 20px;
    border-color: transparent transparent transparent #333;
    margin-left: 4px;
  }

  .video-item:hover .video-play-button {
    transform: translate(-50%, -50%) scale(1.1);
    background: rgba(255, 255, 255, 1);
  }

  .video-placeholder {
    width: 100%;
    aspect-ratio: 16/9;
    background: #f0f0f0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 48px;
    color: #999;
  }

  .video-title {
    padding: 15px;
    font-size: {{ section.settings.content_font_size_desktop }}px;
    line-height: {{ section.settings.content_line_height_desktop }};
  }

  /* Modal for Video */
  .video-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    z-index: 9999;
    align-items: center;
    justify-content: center;
  }

  .video-modal.active {
    display: flex;
  }

  .video-modal-content {
    position: relative;
    width: 90%;
    max-width: 1200px;
  }

  .video-modal-close {
    position: absolute;
    top: -40px;
    right: 0;
    background: none;
    border: none;
    color: white;
    font-size: 32px;
    cursor: pointer;
    padding: 0;
    width: 40px;
    height: 40px;
  }

  .video-player {
    width: 100%;
    aspect-ratio: 16/9;
    position: relative;
  }

  .video-player video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* Fix horizontal scrolling on mobile */
  * {
    box-sizing: border-box;
  }

  .technical-specs-section,
  .technical-specs-container,
  .specs-tab-content,
  .technical-specs-content {
    max-width: 100%;
    overflow-x: hidden;
  }

  @media (max-width: 768px) {
    .technical-specs-section {
      padding: {{ section.settings.padding_top_mobile }}px 0 {{ section.settings.padding_bottom_mobile }}px;
    }

    .technical-specs-title {
      font-size: {{ section.settings.title_font_size_mobile }}px;
      line-height: {{ section.settings.title_line_height_mobile }};
      margin-bottom: 30px;
    }

    .technical-specs-content {
      grid-template-columns: 1fr;
      gap: 30px;
      margin-bottom: 0;
    }

    .specs-tab-content {
      padding: 30px 20px;
      margin: 0px;
    }

    .specs-subtitle {
      font-size: {{ section.settings.subtitle_font_size_mobile }}px;
      line-height: {{ section.settings.subtitle_line_height_mobile }};
      margin-bottom: 15px;
    }

    .specs-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .spec-label {
      font-size: {{ section.settings.content_font_size_mobile }}px;
      line-height: {{ section.settings.content_line_height_mobile }};
    }

    .spec-value {
      font-size: {{ section.settings.content_font_size_mobile }}px;
      line-height: {{ section.settings.content_line_height_mobile }};
    }

    .specs-tabs-wrapper {
      margin-top: 0;
      margin-left: -20px;
      margin-right: -20px;
      padding: 0px;
    }

    .specs-tabs {
      gap: 20px;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none; /* Firefox */
      -ms-overflow-style: none; /* IE and Edge */
    }

    .specs-tabs::-webkit-scrollbar {
      display: none; /* Chrome, Safari, Opera */
    }

    .specs-tab {
      padding: 12px 0;
      font-size: 14px;
      white-space: nowrap;
      flex-shrink: 0;
    }

    .tab-content-title {
      font-size: 24px;
      margin-bottom: 20px;
    }

    .download-label {
      font-size: 14px;
      word-break: break-word;
    }

    .video-player {
      width: 100%;
      aspect-ratio: 16 / 9;
    }

    .video-player video {
      width: 100%;
      height: 100%;
    }

    .download-button {
      font-size: 12px;
      padding: 8px 15px;
      flex-shrink: 0;
    }

    .download-item {
      gap: 10px;
    }

    .faq-question {
      font-size: 13px;
      padding: 20px 0;
    }

    .faq-answer {
      font-size: 13px;
    }

    .faq-answer-content {
      margin-left: 0;
    }

    .video-grid {
      grid-template-columns: 1fr;
    }

    .video-play-button {
      width: 60px;
      height: 60px;
    }

    .video-play-button::after {
      border-width: 10px 0 10px 16px;
    }

    .specs-image {
      width: 100%;
      height: auto;
    }
  }
</style>

<div class="technical-specs-section {{ section.settings.custom_class }}">
  <div class="technical-specs-container">
    {%- if section.settings.title != blank -%}
      <h2 class="technical-specs-title">{{ section.settings.title }}</h2>
    {%- endif -%}

    {%- assign has_specifications = false -%}
    {%- assign has_downloads = false -%}
    {%- assign has_faqs = false -%}
    {%- assign has_videos = false -%}
    {%- assign tab_order = "" -%}

    {%- for block in section.blocks -%}
      {%- if block.type == 'tab_item' -%}
        {%- assign tab_type = block.settings.tab_type -%}
        {%- if tab_order != "" -%}
          {%- assign tab_order = tab_order | append: "," -%}
        {%- endif -%}
        {%- assign tab_order = tab_order | append: tab_type -%}
        
        {%- if tab_type == 'specification' -%}
          {%- assign has_specifications = true -%}
        {%- elsif tab_type == 'pdf' -%}
          {%- assign has_downloads = true -%}
        {%- elsif tab_type == 'faq' -%}
          {%- assign has_faqs = true -%}
        {%- elsif tab_type == 'video' -%}
          {%- assign has_videos = true -%}
        {%- endif -%}
      {%- endif -%}
    {%- endfor -%}

    {%- assign tab_array = tab_order | split: "," -%}

    {%- if tab_order != "" -%}
    <div class="specs-tabs-wrapper">
      <!-- Tabs navigation at the top -->
      <div class="specs-tabs">
        {%- assign first_tab = tab_array[0] -%}
        {%- for tab in tab_array -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'tab_item' and block.settings.tab_type == tab -%}
              <button class="specs-tab{% if tab == first_tab %} active{% endif %} " data-tab="{{ tab }}" {{ block.shopify_attributes }}>
                {{ block.settings.tab_label }}
              </button>
              {%- break -%}
            {%- endif -%}
          {%- endfor -%}
        {%- endfor -%}
      </div>

      {%- if has_specifications -%}
        {%- assign spec_bg_color = "transparent" -%}
        {%- assign spec_text_color = "#333333" -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'tab_item' and block.settings.tab_type == 'specification' -%}
            {%- assign spec_bg_color = block.settings.tab_background_color -%}
            {%- assign spec_text_color = block.settings.tab_text_color -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="specs-tab-content{% if tab_array[0] == 'specification' %} active{% endif %}" data-content="specification" style="background-color: {{ spec_bg_color }}; color: {{ spec_text_color }};">
          <!-- Specification content inside tab -->
          <div class="technical-specs-content">
            <div class="specs-left">
              {%- if section.settings.subtitle != blank -%}
                <h3 class="specs-subtitle">{{ section.settings.subtitle }}</h3>
              {%- endif -%}
              
              <div class="specs-grid">
                {%- for block in section.blocks -%}
                  {%- if block.type == 'tech_spec' -%}
                    <div class="spec-item" {{ block.shopify_attributes }}>
                      <div class="spec-label" style="color: {{ block.settings.label_color }};">
                        {{ block.settings.label }}
                      </div>
                      <div class="spec-value" style="color: {{ block.settings.value_color }};">
                        {{ block.settings.value }}
                      </div>
                    </div>
                  {%- endif -%}
                {%- endfor -%}
              </div>
            </div>

            <div class="specs-right">
              {%- if section.settings.image != blank -%}
                <img 
                  src="{{ section.settings.image | image_url: width: 800 }}" 
                  alt="{{ section.settings.image.alt | escape }}"
                  class="specs-image"
                  width="{{ section.settings.image.width }}"
                  height="{{ section.settings.image.height }}"
                  loading="lazy"
                >
              {%- else -%}
                <div class="video-placeholder">📐</div>
              {%- endif -%}
            </div>
          </div>
        </div>
      {%- endif -%}

      {%- if has_downloads -%}
        {%- assign pdf_bg_color = "transparent" -%}
        {%- assign pdf_text_color = "#333333" -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'tab_item' and block.settings.tab_type == 'pdf' -%}
            {%- assign pdf_bg_color = block.settings.tab_background_color -%}
            {%- assign pdf_text_color = block.settings.tab_text_color -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="specs-tab-content{% if tab_array[0] == 'pdf' %} active{% endif %}" data-content="pdf" style="background-color: {{ pdf_bg_color }}; color: {{ pdf_text_color }};">
          <div class="download-list">
            {%- for block in section.blocks -%}
              {%- if block.type == 'download' -%}
                <div class="download-item" {{ block.shopify_attributes }}>
                  {%- if block.settings.file != blank -%}
                    <a href="{{ block.settings.file }}" class="download-link" download target="_blank" rel="noopener noreferrer">
                      <span class="download-label">{{ block.settings.label }}</span>
                      <span class="download-button">
                        <svg class="download-icon" xmlns="http://www.w3.org/2000/svg" id="Layer_2" width="19" height="19" data-name="Layer 2" viewBox="0 0 114.79 146.52">
                          <g id="Layer_1-2" data-name="Layer 1">
                            <polyline fill="none" stroke="currentColor" stroke-miterlimit="10" stroke-width="1.5px" points="76.09 .75 .75 .75 .75 145.77 114.04 145.77 114.04 43.02"></polyline>
                            <polygon fill="currentColor" points="76.18 42.84 114.54 43.03 76.09 .26 76.18 42.84"></polygon>
                          </g>
                        </svg>
                        Download
                      </span>
                    </a>
                  {%- else -%}
                    <div class="download-link">
                      <span class="download-label">{{ block.settings.label }}</span>
                      <span class="download-button">
                        <svg class="download-icon" xmlns="http://www.w3.org/2000/svg" id="Layer_2" width="19" height="19" data-name="Layer 2" viewBox="0 0 114.79 146.52">
                          <g id="Layer_1-2" data-name="Layer 1">
                            <polyline fill="none" stroke="currentColor" stroke-miterlimit="10" stroke-width="1.5px" points="76.09 .75 .75 .75 .75 145.77 114.04 145.77 114.04 43.02"></polyline>
                            <polygon fill="currentColor" points="76.18 42.84 114.54 43.03 76.09 .26 76.18 42.84"></polygon>
                          </g>
                        </svg>
                        Download
                      </span>
                    </div>
                  {%- endif -%}
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        </div>
      {%- endif -%}

      {%- if has_faqs -%}
        {%- assign faq_bg_color = "transparent" -%}
        {%- assign faq_text_color = "#333333" -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'tab_item' and block.settings.tab_type == 'faq' -%}
            {%- assign faq_bg_color = block.settings.tab_background_color -%}
            {%- assign faq_text_color = block.settings.tab_text_color -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="specs-tab-content{% if tab_array[0] == 'faq' %} active{% endif %}" data-content="faq" style="background-color: {{ faq_bg_color }}; color: {{ faq_text_color }};">
          {%- if section.settings.faq_title != blank -%}
            <h3 class="tab-content-title">{{ section.settings.faq_title }}</h3>
          {%- endif -%}
          <div class="faq-list">
            {%- for block in section.blocks -%}
              {%- if block.type == 'faq' -%}
                <div class="faq-item" {{ block.shopify_attributes }}>
                  <button class="faq-question" style="color: {{ block.settings.question_color }};">
                    <span>{{ block.settings.question }}</span>
                    <span class="faq-icon" style="color: {{ block.settings.question_color }};">›</span>
                  </button>
                  <div class="faq-answer">
                    <div class="faq-answer-content" style="color: {{ block.settings.answer_color }};">
                      {{ block.settings.answer }}
                    </div>
                  </div>
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        </div>
      {%- endif -%}

      {%- if has_videos -%}
        {%- assign video_bg_color = "transparent" -%}
        {%- assign video_text_color = "#333333" -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'tab_item' and block.settings.tab_type == 'video' -%}
            {%- assign video_bg_color = block.settings.tab_background_color -%}
            {%- assign video_text_color = block.settings.tab_text_color -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="specs-tab-content{% if tab_array[0] == 'video' %} active{% endif %}" data-content="video" style="background-color: {{ video_bg_color }}; color: {{ video_text_color }};">
          <div class="video-grid">
            {%- for block in section.blocks -%}
              {%- if block.type == 'video' -%}
                <div class="video-item" data-video-url="{{ block.settings.video_url }}" {{ block.shopify_attributes }}>
                  <div class="video-player" data-type="desktop">
                    {%- if block.settings.video_url contains '.mp4'
                      or block.settings.video_url contains '.webm'
                      or block.settings.video_url contains '.ogg'
                      or block.settings.video_url contains '.mov'
                    -%}
                      <video
                        x5-video-player-type="h5"
                        playsinline
                        controls
                        class="video"
                        poster="{% if block.settings.thumbnail != blank %}{{ block.settings.thumbnail | image_url: width: 1500 }}{% endif %}"
                        preload="metadata"
                      >
                        <source
                          src="{{ block.settings.video_url }}"
                          type="video/mp4"
                        >
                        Your browser does not support the video tag.
                      </video>
                    {%- else -%}
                      <div class="video-thumbnail-wrapper">
                        {%- if block.settings.thumbnail != blank -%}
                          <img 
                            src="{{ block.settings.thumbnail | image_url: width: 600 }}" 
                            alt="{{ block.settings.title | escape }}"
                            class="video-thumbnail"
                            width="{{ block.settings.thumbnail.width }}"
                            height="{{ block.settings.thumbnail.height }}"
                            loading="lazy"
                          >
                        {%- else -%}
                          <div class="video-placeholder">▶</div>
                        {%- endif -%}
                        <div class="video-play-button"></div>
                      </div>
                    {%- endif -%}
                  </div>
                  {%- if block.settings.title != blank -%}
                    <div class="video-title" style="background-color: {{ block.settings.title_background_color }}; color: {{ block.settings.title_color }};">{{ block.settings.title }}</div>
                  {%- endif -%}
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        </div>
      {%- endif -%}
    </div>
    {%- endif -%}
  </div>
</div>

<div class="video-modal" id="videoModal">
  <div class="video-modal-content">
    <button class="video-modal-close" id="closeVideoModal">&times;</button>
    <div id="videoPlayerContainer"></div>
  </div>
</div>

<script>
(() => {
  // 独立作用域，避免变量冲突
  document.addEventListener('DOMContentLoaded', function() {

    /* -------------------- Tab switching -------------------- */
    document.querySelectorAll('.specs-tab').forEach(tab => {
      tab.addEventListener('click', function() {
        const tabName = this.getAttribute('data-tab');
        
        document.querySelectorAll('.specs-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.specs-tab-content').forEach(c => c.classList.remove('active'));
        
        this.classList.add('active');
        const targetContent = document.querySelector(`[data-content="${tabName}"]`);
        if (targetContent) targetContent.classList.add('active');
      });
    });

    /* -------------------- FAQ accordion -------------------- */
    document.querySelectorAll('.faq-question').forEach(question => {
      question.addEventListener('click', function() {
        const faqItem = this.closest('.faq-item');
        const isActive = faqItem.classList.contains('active');
        document.querySelectorAll('.faq-item').forEach(item => item.classList.remove('active'));
        if (!isActive) faqItem.classList.add('active');
      });
    });

    /* -------------------- Video modal -------------------- */
    const videoModal = document.getElementById('videoModal');
    const closeVideoModal = document.getElementById('closeVideoModal');
    const videoPlayerContainer = document.getElementById('videoPlayerContainer');

    if (videoModal && videoPlayerContainer) {
      // 打开视频（仅适用于 YouTube/Vimeo）
      document.querySelectorAll('.video-item').forEach(item => {
        const thumbnailWrapper = item.querySelector('.video-thumbnail-wrapper');
        if (thumbnailWrapper) {
          thumbnailWrapper.addEventListener('click', function(e) {
            e.stopPropagation();
            const videoUrl = item.getAttribute('data-video-url');
            if (!videoUrl) return;

            // 只处理 YouTube 和 Vimeo 链接
            if (!videoUrl.includes('youtube.com') && !videoUrl.includes('youtu.be') && !videoUrl.includes('vimeo.com')) {
              return;
            }

            let embedUrl = videoUrl;
            // ✅ 支持 YouTube / Shorts
            if (videoUrl.includes('youtube.com') || videoUrl.includes('youtu.be')) {
              const match = videoUrl.match(/(?:v=|\/(?:shorts|embed|v)\/)([a-zA-Z0-9_-]{11})/);
              if (match && match[1]) {
                embedUrl = `https://www.youtube.com/embed/${match[1]}?autoplay=1`;
              }
            }
            // ✅ 支持 Vimeo
            if (videoUrl.includes('vimeo.com')) {
              const match = videoUrl.match(/vimeo\.com\/(\d+)/);
              if (match && match[1]) {
                embedUrl = `https://player.vimeo.com/video/${match[1]}?autoplay=1`;
              }
            }

            // 插入 iframe 播放
            videoPlayerContainer.innerHTML =
              `<iframe class="video-player" src="${embedUrl}" frameborder="0"
               allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe>`;

            // 打开模态框
            videoModal.classList.add('active');
          });
        }
      });

      // 关闭视频函数（供多个事件调用）
      const closeVideo = () => {
        videoModal.classList.remove('active');
        videoPlayerContainer.innerHTML = '';
      };

      // 点击关闭按钮
      closeVideoModal?.addEventListener('click', closeVideo);

      // 点击背景关闭
      videoModal.addEventListener('click', function(e) {
        if (e.target === videoModal) closeVideo();
      });

      // ✅ 按 ESC 键关闭视频
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && videoModal.classList.contains('active')) {
          closeVideo();
        }
      });
    }
  });
})();
</script>

{% schema %}
{
  "name": "Technical Specifications",
  "tag": "section",
  "class": "section-technical-specifications",
  "settings": [
    {
      "type": "header",
      "content": "Section Settings"
    },
    {
      "type": "text",
      "id": "custom_class",
      "label": "Custom CSS Class",
      "info": "Add custom CSS class for additional styling"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Section Background Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "container_width",
      "min": 800,
      "max": 2000,
      "step": 50,
      "unit": "px",
      "label": "Container Max Width",
      "default": 1400,
      "info": "Maximum width of the content container"
    },
    {
      "type": "header",
      "content": "Section Spacing - Desktop"
    },
    {
      "type": "range",
      "id": "padding_top_desktop",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "Top Padding",
      "default": 60,
      "info": "Space above the section on desktop"
    },
    {
      "type": "range",
      "id": "padding_bottom_desktop",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 60,
      "info": "Space below the section on desktop"
    },
    {
      "type": "header",
      "content": "Section Spacing - Mobile"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "Top Padding",
      "default": 40,
      "info": "Space above the section on mobile"
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 40,
      "info": "Space below the section on mobile"
    },
    {
      "type": "header",
      "content": "Content Settings"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Technical Specifications"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Generic Data"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Technical Drawing/Image"
    },
    {
      "type": "text",
      "id": "faq_title",
      "label": "FAQ Section Title",
      "default": "Frequently Asked Questions"
    },
    {
      "type": "header",
      "content": "Typography Settings - Desktop"
    },
    {
      "type": "range",
      "id": "title_font_size_desktop",
      "min": 16,
      "max": 72,
      "step": 1,
      "unit": "px",
      "label": "Main Title - Font Size",
      "default": 32,
      "info": "Font size for 'Technical Specifications' title on desktop"
    },
    {
      "type": "range",
      "id": "title_line_height_desktop",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Main Title - Line Height",
      "default": 1.2,
      "info": "Line height multiplier for main title on desktop"
    },
    {
      "type": "range",
      "id": "subtitle_font_size_desktop",
      "min": 14,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Subtitle - Font Size",
      "default": 24,
      "info": "Font size for subtitle (e.g., 'Generic Data') on desktop"
    },
    {
      "type": "range",
      "id": "subtitle_line_height_desktop",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Subtitle - Line Height",
      "default": 1.4,
      "info": "Line height multiplier for subtitle on desktop"
    },
    {
      "type": "range",
      "id": "content_font_size_desktop",
      "min": 10,
      "max": 32,
      "step": 1,
      "unit": "px",
      "label": "Content - Font Size",
      "default": 14,
      "info": "Font size for spec labels, values, and general content on desktop"
    },
    {
      "type": "range",
      "id": "content_line_height_desktop",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Content - Line Height",
      "default": 1.6,
      "info": "Line height multiplier for content text on desktop"
    },
    {
      "type": "range",
      "id": "tab_font_size_desktop",
      "min": 12,
      "max": 32,
      "step": 1,
      "unit": "px",
      "label": "Tab Button - Font Size",
      "default": 16,
      "info": "Font size for tab buttons on desktop"
    },
    {
      "type": "range",
      "id": "tab_line_height_desktop",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Tab Button - Line Height",
      "default": 1.5,
      "info": "Line height multiplier for tab buttons on desktop"
    },
    {
      "type": "header",
      "content": "Typography Settings - Mobile"
    },
    {
      "type": "range",
      "id": "title_font_size_mobile",
      "min": 14,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Main Title - Font Size",
      "default": 24,
      "info": "Font size for 'Technical Specifications' title on mobile"
    },
    {
      "type": "range",
      "id": "title_line_height_mobile",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Main Title - Line Height",
      "default": 1.2,
      "info": "Line height multiplier for main title on mobile"
    },
    {
      "type": "range",
      "id": "subtitle_font_size_mobile",
      "min": 12,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Subtitle - Font Size",
      "default": 18,
      "info": "Font size for subtitle (e.g., 'Generic Data') on mobile"
    },
    {
      "type": "range",
      "id": "subtitle_line_height_mobile",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Subtitle - Line Height",
      "default": 1.4,
      "info": "Line height multiplier for subtitle on mobile"
    },
    {
      "type": "range",
      "id": "content_font_size_mobile",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Content - Font Size",
      "default": 12,
      "info": "Font size for spec labels, values, and general content on mobile"
    },
    {
      "type": "range",
      "id": "content_line_height_mobile",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Content - Line Height",
      "default": 1.6,
      "info": "Line height multiplier for content text on mobile"
    },
    {
      "type": "range",
      "id": "tab_font_size_mobile",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Tab Button - Font Size",
      "default": 14,
      "info": "Font size for tab buttons on mobile"
    },
    {
      "type": "range",
      "id": "tab_line_height_mobile",
      "min": 1,
      "max": 3,
      "step": 0.1,
      "label": "Tab Button - Line Height",
      "default": 1.5,
      "info": "Line height multiplier for tab buttons on mobile"
    }
  ],
  "blocks": [
    {
      "type": "tab_item",
      "name": "Tab Item",
      "settings": [
        {
          "type": "select",
          "id": "tab_type",
          "label": "Tab Type",
          "options": [
            {
              "value": "specification",
              "label": "Specification"
            },
            {
              "value": "pdf",
              "label": "PDF Downloads"
            },
            {
              "value": "faq",
              "label": "FAQ"
            },
            {
              "value": "video",
              "label": "Installation Video"
            }
          ],
          "default": "specification",
          "info": "Select the type of content for this tab"
        },
        {
          "type": "text",
          "id": "tab_label",
          "label": "Tab Label",
          "default": "Specification",
          "info": "The text displayed on the tab button"
        },
        {
          "type": "color",
          "id": "tab_background_color",
          "label": "Tab Content Background Color",
          "default": "transparent",
          "info": "Background color for this tab's content area"
        },
        {
          "type": "color",
          "id": "tab_text_color",
          "label": "Tab Content Text Color",
          "default": "#333333",
          "info": "Text color for this tab's content area"
        }
      ]
    },
    {
      "type": "tech_spec",
      "name": "Technical Spec Item",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "WEIGHT (KG)"
        },
        {
          "type": "color",
          "id": "label_color",
          "label": "Label Color",
          "default": "#333333"
        },
        {
          "type": "textarea",
          "id": "value",
          "label": "Value",
          "default": "12,6"
        },
        {
          "type": "color",
          "id": "value_color",
          "label": "Value Color",
          "default": "#666666"
        }
      ]
    },
    {
      "type": "download",
      "name": "Download Item",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Product datasheets",
          "info": "e.g., Product datasheets, Instructions, Care guide"
        },
        {
          "type": "url",
          "id": "file",
          "label": "File URL",
          "info": "Upload the file to Files section and paste the URL here"
        }
      ]
    },
    {
      "type": "faq",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "HOW DO I INSTALL CORSTON ELECTRICAL PRODUCTS?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>You can view and print instructions from our mobile-friendly support page. We recommend using a professional to fit all Corston products. Electrical items must be installed by a qualified electrician.</p>"
        },
        {
          "type": "color",
          "id": "question_color",
          "label": "Question Text Color",
          "default": "#000000"
        },
        {
          "type": "color",
          "id": "answer_color",
          "label": "Answer Text Color",
          "default": "#666666"
        }
      ]
    },
    {
      "type": "video",
      "name": "Video Item",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Video Title",
          "default": "Installation Guide"
        },
        {
          "type": "url",
          "id": "video_url",
          "label": "Video URL",
          "info": "YouTube or Vimeo URL"
        },
        {
          "type": "image_picker",
          "id": "thumbnail",
          "label": "Video Thumbnail"
        },
        {
          "type": "color",
          "id": "title_color",
          "label": "Title Text Color",
          "default": "#333333"
        },
        {
          "type": "color",
          "id": "title_background_color",
          "label": "Title Background Color",
          "default": "#FFFFFF"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Technical Specifications",
      "blocks": [
        {
          "type": "tab_item",
          "settings": {
            "tab_type": "specification",
            "tab_label": "Specification",
            "tab_background_color": "transparent",
            "tab_text_color": "#333333"
          }
        },
        {
          "type": "tab_item",
          "settings": {
            "tab_type": "pdf",
            "tab_label": "PD",
            "tab_background_color": "#E3F2FD",
            "tab_text_color": "#333333"
          }
        },
        {
          "type": "tab_item",
          "settings": {
            "tab_type": "faq",
            "tab_label": "FAQ",
            "tab_background_color": "transparent",
            "tab_text_color": "#333333"
          }
        },
        {
          "type": "tab_item",
          "settings": {
            "tab_type": "video",
            "tab_label": "Installation",
            "tab_background_color": "#000000",
            "tab_text_color": "#FFFFFF"
          }
        },
        {
          "type": "tech_spec",
          "settings": {
            "label": "WEIGHT (KG)",
            "value": "12,6"
          }
        },
        {
          "type": "tech_spec",
          "settings": {
            "label": "DIMENSIONS (CM)",
            "value": "120"
          }
        },
        {
          "type": "tech_spec",
          "settings": {
            "label": "FINISHING",
            "value": "Stainless Steel"
          }
        },
        {
          "type": "download",
          "settings": {
            "label": "Product datasheets"
          }
        },
        {
          "type": "download",
          "settings": {
            "label": "Instructions"
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "HOW DO I INSTALL CORSTON ELECTRICAL PRODUCTS?",
            "answer": "<p>You can view and print instructions from our mobile-friendly support page. We recommend using a professional to fit all Corston products.</p>",
            "question_color": "#000000",
            "answer_color": "#666666"
          }
        },
        {
          "type": "video",
          "settings": {
            "title": "Installation Guide",
            "title_color": "#333333",
            "title_background_color": "#FFFFFF"
          }
        }
      ]
    }
  ]
}
{% endschema %}

